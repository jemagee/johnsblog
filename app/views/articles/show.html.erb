<h1 class="page-header">
  <%= @article.title %>
</h1>

  <% if !@article.category.nil? %>
    <h3>
      Category: <%= @article.category.name %>
    </h3>
  <% end %>


<div class="abstract">
  Abstract: <%= @article.abstract %>
</div>

<div class="article-body">
  <h4>Article Body</h4>
  <%= @article.body %>
</div>
<% if @article.status == "published" %>
  <%= link_to "Add Comment", new_article_comment_path(@article) %>
  <% @article.comments.each do |comment| %>
    <div id="comment-<%= comment.id %>" >
      <%= comment.title %>
      <%= comment.body %>
      <%= link_to "Edit Comment", [:edit, @article, comment] %>
      <%= link_to "Delete Comment", [@article, comment], method: :delete, data: {confirm: "Are you sure you want to delete this comment"} %>
    </div>
  <% end %>
<% end %>